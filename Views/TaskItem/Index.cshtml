@model List<TaskItem>

<h1>Task Items</h1>

@if (Model.Count > 0)
{
    foreach (TaskItem task in Model)
    {
        <div class="panel">
            <div class="panel-title"></div>
            <div class="panel-body"></div>
        </div>
    }
} else
{
    <h3>No Tasks Exist...</h3>
}

